package task8;

public class FunctionLogic {
    public double function(double x) { // Задайте значение переменной x
        double result;
        if (x >= 3) {
            return result = -x * x + 3 * x + 9;
        } else {
            return result = 1 / (x * x * x);
        }
    }
}
